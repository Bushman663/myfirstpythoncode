#a function that calculates the squareroot of a number
from math import sqrt
def main():
    num = int(input("Enter a number: "))
    result = sqrt(num)
    print(f"The square root of {num} is {result}" )

main()
    
